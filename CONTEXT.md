# ズーム遷移ゲーム — 実装コンテキスト

`zoom-swap-chain.html` と一緒にこのファイルを Claude Code に渡すこと。
単一HTMLファイル、依存ライブラリなし、Canvas 2D のみ。

---

## 1. 何を作っているか

スマホ/タブレット前提の、**ピンチイン/ピンチアウトで異なるイラスト間を遷移する**ギミックのプロトタイプ。
現状は「カエルタコイカ案」——**ズームアウトすると周り全体のイラストが変わっている**モードのみ実装。

将来的にもう1つ「異変モード」（絵の一部を見る → 引くとどこかがずれている → そこを見るとまたずれている、の連鎖）
を同じ基盤の上に載せる予定。異変モードは**同じアンカー構造にレイヤー差分を置くだけ**で実現できる設計にしてある。

### 遷移の連鎖（1周）

```
カエルの目 →(寄る)→ タコの目 →(ハエ移動)→ タコの吸盤
          →(寄る)→ イカの吸盤 →(ハエ移動)→ イカの別の吸盤
          →(寄る)→ カエルの指の吸盤 →(ハエ移動)→ カエルの目 → ループ
```

「寄る」＝プレイヤーがピンチアウトして倍率が閾値を超えると、背後の生き物がクロスフェードで入れ替わる。
入れ替わりは**寄りきった状態で起きる**ので、引いて初めて「別の生き物になっている」ことに気づく。これが体験の核。

### ハエの役割

ハエは**次にズームインすべき場所を指すマーカー**であり、同時に**クロスフェードのミスディレクション**でもある。

- 常時ウロウロはしない。生き物が切り替わった後、プレイヤーが引いたタイミングで**1回だけ飛んで、その場に固定**される
- 飛ぶ条件：`armed === false`（切り替わり確定済み）かつ `zoom < MOVE_OUT` かつ `MOVE_WAIT` 秒経過
- 切り替わり確定後まだ寄ったままだと、ハエが激しく羽ばたいて「引け」と催促する（UIがないので状態伝達の唯一の手段）

---

## 2. 座標系とアンカー — ここが一番の肝

### ワールド原点 = 現在のズーム対象

各生き物は自分のローカル座標で描かれる。`anchors` に名前付きの特徴点を持ち、
**そのうち1つがワールド原点に来るように平行移動して描画される**。

```js
CREATURES = [
  { draw:drawFrog,  anchors:{ eye:[160,-262],  toe:[-500,400] } },
  { draw:drawOcto,  anchors:{ eye:[-165,-430], sucker:[225,210] } },
  { draw:drawSquid, anchors:{ sucker:[240,195], sucker2:[475,456] } },
];
```

描画時：`ctx.translate(-anchor[0], -anchor[1])` してから `draw()` を呼ぶ。
つまり **worldPos = localPos − anchor**。

### 連鎖テーブル

```js
CHAIN = [
  { c:0, land:'toe',    zoom:'eye'     },  // カエル：指の吸盤に着地 → 目へ移動
  { c:1, land:'eye',    zoom:'sucker'  },  // タコ  ：目に着地      → 吸盤へ移動
  { c:2, land:'sucker', zoom:'sucker2' },  // イカ  ：吸盤に着地    → 別の吸盤へ移動
];
```

- `land` = その生き物に切り替わった瞬間にハエが居る特徴（＝前の生き物の `zoom` と重なる特徴）
- `zoom` = ハエが移動した先。次にズームインする特徴

`activeAnchor` が現在ワールド原点に来ている特徴名を保持する。

### 再アンカー

ハエが着地したら座標系を付け替える。アンカーを A1→A2 に変えると全ワールド座標が `(A1 - A2)` ずれるので、
カメラとハエも同量ずらして**見た目を1ピクセルも動かさない**。

```js
function reanchor(name){
  const A1 = cr.anchors[activeAnchor], A2 = cr.anchors[name];
  const dx = A1[0]-A2[0], dy = A1[1]-A2[1];
  camX+=dx; camY+=dy;
  fly.x+=dx; fly.y+=dy; fly.fx+=dx; fly.fy+=dy; fly.tx+=dx; fly.ty+=dy;
  activeAnchor = name;
}
```

**この関数を壊すと画面がワープする。触る前に上の式を必ず理解すること。**

---

## 3. ズーム

### 倍率は必ず log2 で持つ

`zoom` は log2 スケール。実倍率は `2^zoom`。線形の scale で持つと層ごとに 1/50, 1/2500… と桁が飛んで
閾値もイージングも手に負えなくなる。log2 なら層が等間隔に並び、閾値が人間に読める数字になる。

```js
scale() = 2^zoom * fit()          // fit() = min(W,H)/1150
```

ワールド1150ユニットが zoom 0 で画面短辺に収まる。

### 入力

- **ピンチ**：Pointer Events で自前実装。**補間を挟まず指に1:1で追従**させる（遅れると即バレる）
- **ホイール**：離散イベントなので `zoomTarget` を動かし、毎フレーム指数減衰で `zoom` を寄せる

```js
setZoom(zoom + (zoomTarget-zoom)*(1-Math.exp(-dt/smoothTau)), smoothAX, smoothAY);
```

`1-exp(-dt/tau)` を使うこと。単純な `lerp(a,b,0.15)` は**フレームレートで速度が変わる**（120Hzで倍速になる）。

`deltaMode` の正規化必須（Firefox は行単位＝1で来る）。トラックパッドのピンチは `ctrlKey` 付き wheel。

### ブラウザ標準ズームとの競合回避

3点セットで潰している。**どれも外さないこと。**

1. `canvas { touch-action: none; }` ← これが本命。iOS Safari 13+ で効く
2. `gesturestart / gesturechange / gestureend` を `preventDefault`（iOS Safari 独自API の保険）
3. `wheel` を `{passive:false}` で `preventDefault`（デスクトップのブラウザ拡大を阻止）

`<meta viewport user-scalable=no>` は **iOS Safari 10 以降で完全に無視される**ので使っていない。
`body { position:fixed; overflow:hidden; overscroll-behavior:none; }` でバウンススクロールも止めている。

### カメラ

`clampCam()` が原点（＝現在のズーム対象）からの距離を制限する。寄るほど半径が小さくなり、迷子を防ぐ。
ハエが飛んでいる間は、指が離れていればカメラが緩く追従する（`camX += (fly.x-camX)*0.05`）。

---

## 4. クロスフェード

```js
paint(node, activeAnchor, 1);                    // A は不透明で描く
if(blend>0.001) paint(next, CHAIN[next].land, blend);   // B を alpha=blend で重ねる
```

**A も B も α を振ってはいけない。** 両方αにすると中間で背景が透けて濁る（A が (1-t)² で効いてしまう）。
A を不透明・B を alpha=t にすると `result = A*(1-t) + B*t` の綺麗なクロスフェードになる。

### 進行のラッチ

```js
const t = smoothstep(swapLo, SWAP_HI, zoom);
if(armed && t >= 0.999){                  // 寄りきったら確定
  node = (node+1) % CHAIN.length;
  activeAnchor = CHAIN[node].land;        // B は既にこの基準で描かれているので継ぎ目なし
  armed = false; moveWait = MOVE_WAIT;
}
blend = armed ? t : 0;
```

確定の瞬間、直前は「B を alpha=1 で重ねた状態」、直後は「B を alpha=0 で不透明描画」＝**同じ絵**。よって継ぎ目が出ない。
`armed` はハエが次の場所に着地するまで `false` のままなので、誤って同じ場所で二重に進行しない。

---

## 5. 手描きスケッチ風の描画

白黒基調。紙の色（`PAPER`）は3種とも**完全に同一**。変わるのは紙に描かれた絵だけ。
明度帯も3種で揃えてあり、フェード中の手がかりを**形だけ**に絞っている（色が違うと入れ替わりが先にバレる）。

### 固定ノイズ

```js
function n(i){ const x=Math.sin(i*127.1+311.7)*43758.5453; return (x-Math.floor(x))*2-1; }
```

**毎フレーム `Math.random()` を振ってはいけない。** 線が沸いて（アニメの「ガタガタ」）見ていられなくなる。
すべての手描き揺らぎは種（seed）から決まる固定値。各シェイプに固有の seed 番号を振ってある。

### 描画プリミティブ

- `roughEll(cx,cy,rx,ry,rot,seed,amp)` — 26点サンプリングして半径をノイズで揺らした楕円パス
- `roughArm(x1,y1,cx,cy,x2,y2,w,seed)` — 二次ベジエに沿って太さが先細るシルエットパス（腕・脚用）
- `ink(pathFn, wash, ang, spacing, seed, lw, ext)` — 塗り → クリップしてハッチング → インク輪郭 の3層を一括
- `at(x,y,fn)` — translate して fn を呼ぶだけのヘルパ

### 線幅は画面px固定

```js
PX = 1/scale();                 // 毎フレーム更新
ctx.lineWidth = 2.6 * PX;       // ワールド変換内で「画面上 2.6px」になる
```

これで**どの倍率でも1枚の手描き絵に見える**。ついでに解像度の破綻も誤魔化せる。
ハエだけは画面座標で描くので、描画中に一時的に `PX = 1` に差し替えている（`drawFly` 内）。

### 性能

`ink()` の `ext` 引数がハッチングの走査範囲で、**描画量に直結する**。形ごとに必要最小限を渡すこと。
初期実装で全部 R=4000 にしていたら1フレーム数千ストロークになって落ちた。現在は数百。

紙目は 180px のノイズタイルを `globalCompositeOperation='multiply'` で全面に乗せている。
**全レイヤーの上**に来るので、クロスフェード中も紙の質感だけは動かない（2枚の絵が同じ紙の上の出来事に見える）。

---

## 6. チューニング定数

ファイル冒頭にまとめてある。

| 定数 | 現在値 | 意味 |
|---|---|---|
| `MIN_ZOOM` / `MAX_ZOOM` | -0.45 / 5.4 | 倍率の下限・上限（log2） |
| `SWAP_HI` | 4.35 | ここまで寄ると入れ替わり確定（実倍率 ×20.4） |
| `FADE_W` | 0.8 | クロスフェードにかける zoom 幅。`swapLo = SWAP_HI - FADE_W` |
| `MOVE_OUT` | 0.75 | ここまで引くとハエが次の場所へ飛ぶ（実倍率 ×1.68） |
| `MOVE_WAIT` | 0.7 | 引いてから飛び立つまでの秒数（驚きを認識させる間） |
| `smoothTau` | 0.15 / 0.05 | ホイール補間の時定数（通常 / trackpadピンチ） |

---

## 7. UI

**画面上部中央の拡大率表示だけ。** それ以外のUIは意図的に置いていない。
`mix-blend-mode: multiply` で紙に薄く刷った見た目にしてある。
`env(safe-area-inset-top)` でノッチを避けている。

---

## 8. 本番イラストへの差し替え手順

1. `drawFrog / drawOcto / drawSquid` を新しい描画に置き換える（ローカル座標で自由に描いてよい）
2. `CREATURES[i].anchors` の座標を、新しい絵の特徴点の位置に更新する
3. アンカーになる特徴は**すべて半径58前後・同じ明度帯**に揃えること（錯視の成立条件）
4. 連鎖を増やすなら `CREATURES` と `CHAIN` に足すだけ。他のコードは触らなくてよい

ラスタ画像に差し替える場合は、倍率が最大 ×42 まで行くので**層ごとに解像度の違う素材を用意し、
スケール閾値でテクスチャを差し替える（LOD切り替え＋クロスフェード）**必要がある。
現在のベクタ描画はこの問題を回避するための選択。

---

## 9. 未検証・要判断

- カエルの目 → タコの目 が錯視として一番効くはず（実際どちらも横長瞳孔）。ここで驚きが出るか
- イカの吸盤 → 別の吸盤 の移動が退屈でないか。同じ生き物内の移動なので発見が薄い可能性がある
- カエルの指の吸盤（趾間板）を吸盤寄りに寄せすぎていないか。錯視優先か生物としての自然さか
- `FADE_W` の最適値。短くパッと切り替える方が強いか、長く溶かす方が強いか未決着
- ハエを消したときにフェードがバレるか（＝ミスディレクションが機能しているか）の検証がまだ
- スマホ実機で60fps出るか。落ちるならハッチング間隔を広げるか、遠景層のハッチを省く
